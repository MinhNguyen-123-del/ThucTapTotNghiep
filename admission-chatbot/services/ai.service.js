// Service xử lý AI
// Sau này sẽ kết nối API AI vào đây

const askAI = async (question) => {
    // Công dụng: nhận câu hỏi và trả về câu trả lời AI

    return `AI đang xử lý câu hỏi: ${question}`;
};

module.exports = {
    askAI
};